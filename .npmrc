# Keep install logs clean on CI/deploys: transitive-dependency deprecation
# notices (from eslint 8's tree, pinned by eslint-config-next 14) are noise we
# can't act on without a major-version migration. Errors still surface.
loglevel=error
fund=false
audit=false
